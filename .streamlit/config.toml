[theme]
primaryColor = "#2563EB"              # Blue (buttons, sliders)
backgroundColor = "#0F172A"          # Dark background
secondaryBackgroundColor = "#1E293B" # Cards / sections
textColor = "#F8FAFC"                # White text (high visibility)
font = "sans serif"
